### 2026-03-03T14:32:00Z: User directive
**By:** Jorge Balderas (via Copilot)
**What:** Make the `local` (H2) profile the default Spring profile, so developers don't need to specify `-Dspring-boot.run.profiles=local`.
**Why:** User request — captured for team memory
